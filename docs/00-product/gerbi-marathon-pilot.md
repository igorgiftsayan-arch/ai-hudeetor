# Герби-Марафон — нормативный scope пилота

## Цель

Проверить ежедневный командный сценарий внутри существующего AI-худеетора без
переименования приложения и без построения общей социальной сети. По уточнению
владельца от 2026-09-24 фото еды, Web Push на телефон и автоматическое AI recovery
обязательны в этом же пилоте. Ранние исключения и позиции в поздней roadmap
не отменяют это решение; остальные границы V1 сохраняются.

## Основной пользовательский путь

1. Завершённый пользователь входит в конкретный марафон и ровно одну команду.
2. Вес вводится и исправляется только через существующий tracking daily upsert.
3. Пользователь отправляет отчёт **за вчера** по восьми фиксированным отметкам.
4. Капитан публикует отдельное задание на локальную дату команды.
5. Участник отмечает или отменяет выполнение задания.
6. Экран команды получает только дневные, безопасные производные результаты.

## Восемь отметок отчёта

1. Утренний коктейль.
2. Физическая активность.
3. Норма воды.
4. Второй коктейль.
5. Здоровый ужин.
6. Хороший сон.
7. Никакой вредной еды.
8. Никакого курения.

Отчёт отсутствует, пока пользователь его не отправил. Отсутствие отчёта —
`unknown`, а не нулевой результат. Повторная отправка за ту же дату обновляет
тот же отчёт.

## Три независимых показателя

- дневной процент веса: `(вес вчера − вес сегодня) / вес вчера × 100%` только по точным соседним локальным датам;
- дневной Веллнес показатель: один балл за каждую из восьми отметок (`0..8`);
- выполнение задания капитана, которое участник отмечает сам.

Общий score, веса показателей и единый победитель запрещены. Накопленные итоги
марафона не выдаются командному API. Задание капитана не является девятой
отметкой Веллнес отчёта. При равном значении участники занимают общее место и
показываются одной группой; произвольный победитель не выбирается.

Стартовый вес фиксируется один раз по первой актуальной дневной записи
пользователя внутри периода марафона. Последующее исправление daily weight не
меняет уже зафиксированный baseline. Для дневного процента отсутствие точной
записи вчера или сегодня означает `unknown`; более старая запись не подставляется.

## Доступ и приватность

- участник видит только команду, в которой состоит;
- капитан имеет полномочия только внутри своей команды;
- командный read model не содержит raw weight, AI conversation, AI memory,
  email или иных скрытых профильных данных;
- API возвращает только дневные производные метрики, разрешённое display name и
  состояние отчёта/задания;
- создание тестового марафона выполняется только через явно включённый
  bootstrap-контур; stable по умолчанию его не включает.

## AI и согласие

GenAPI остаётся за существующим `AiProviderAdapter`. Внешний запрос разрешён
только после сохранённого `aiProviderProcessing` consent актуальной версии.
Fake adapter сохраняется. Timeout после возможной передачи провайдеру остаётся
`outcomeUnknown` и не считается безопасным повтором.

## Не входит

- публичная социальная сеть, лента, реакции и передача токенов;
- общий рейтинг или накопленный командный итог;
- доступ капитана к raw weight, чату или памяти;
- полноценная admin platform;
- произвольные уведомления без opt-in, выбранного времени и активной push subscription;
- автоматическое признание фотографии фактом употребления;
- медицинская оценка, причинные выводы и придуманные нормы питания.

## Открытые решения

До ответа владельца не создаются реальные даты марафона, состав команд и
назначение капитанов. Накопленные итоги скрыты от команды; это ограничение
раскрытия, а не утверждение об отсутствии накопленных данных. Единый общий score
не вводится; дополнительный расчёт итогов этим уточнением не заказывается.

- Домен владельца с направлением на `5.42.126.71` и доверенным HTTPS: `TBD`.
  Тип телефона и фактическая iOS/Android проверка: `TBD`.
- Дополнительные правила питания Герби сверх уже известного профиля: `TBD`.
  Их отсутствие не заменяется придуманной программой питания.
- Политика goodwill-компенсации при неразрешимом pre-request-ID исходе: `TBD`.
  До отдельного ответа она не разрешает автоматический refund или resubmit.

## Food vision — обязательный объём расширенного пилота

Фото еды входит в расширенный pilot scope со следующими границами:

- приватную загрузку в S3-compatible storage и короткоживущие signed URLs;
- MIME/размер validation, quarantine/malware boundary, lifecycle, retention и удаление оригинала;
- provider-neutral vision contract; конкретный adapter остаётся сменяемым;
- цену действия, reservation/confirmation/refund и поведение при `outcomeUnknown`;
- схему результата с неопределённостью распознавания и пользовательской коррекцией до подтверждения;
- privacy/consent, безопасные logs и запрет передачи изображения через analytics/outbox.

В принимаемом пользовательском пути анализ выполняется реальным GenAPI через существующий adapter; fake остаётся инструментом проверки и не заменяет real-provider acceptance. Анализ фиксирует распознанное блюдо/продукты и сообщает соответствие только реально известным целям, ограничениям или правилам с явным источником. Если данных недостаточно, результат должен запросить уточнение или вернуть `insufficientData`. Содержание правил Герби пока не задано и не должно импровизироваться.

Состояния анализа и употребления разделены. Успешный анализ не означает, что пользователь это съел. После результата пользователь отдельно отвечает на вопрос «Съели ли вы это?» и явно подтверждает состав и локальные дату/время; только идемпотентная confirmation-команда создаёт запись дневника. Неподтверждённые анализы доступны отдельно. Пользователь видит подтверждённую историю, исправляет состав и время либо удаляет свою запись; изменения сохраняются после reload. Исправление и удаление owner-scoped; удалённые либо неподтверждённые данные не входят в AI memory/context как факты употребления.

В дальнейшем подтверждённые записи можно сопоставлять с актуальным дневным рядом веса как наблюдаемую индивидуальную динамику. При недостаточной выборке возвращается `insufficientData`; причинные диагнозы и обещания результата запрещены. Это направление дальнейших наблюдений, а не заявление о готовом аналитическом экране; точный UI и достаточная выборка остаются `TBD`.

## Push-напоминания — обязательный объём расширенного пилота

- Канал пилота: Web Push на телефон через установленную PWA там, где платформа поддерживает это безопасно.
- Пользователь сам включает напоминания, предоставляет browser permission и выбирает время; backend не выдаёт разрешение от имени браузера.
- Preference, subscription/device, timezone schedule, revoke/expiry и доставка хранятся раздельно и owner-scoped.
- Расписание аккаунта сохраняет `enabled`, `localTime`, `timezone`. UI отдельно
  определяет `Notification.permission` и статус endpoint текущего устройства;
  число подписок аккаунта не доказывает разрешение этого телефона.
- Подключение и отключение конкретного устройства доступны явно. Logout/смена
  аккаунта не должны оставлять доставку предыдущему аккаунту на этом устройстве.
- Worker delivery идемпотентен по subscription, типу напоминания и запланированному
  моменту. Service worker использует стабильный `deliveryId` для дедупликации и
  notification tag, показывает текст без личных данных и открывает маршрут приложения.
- `denied`, `unsupported`, отозванная/истёкшая subscription и смена timezone являются явными состояниями, а не молчаливым успехом.
- HTTP test stand не доказывает mobile push. Acceptance требует доверенный HTTPS origin и отдельную проверку установленной PWA/iOS Home Screen, если используется iPhone.

## Автоматическое AI recovery — обязательный объём расширенного пилота

Recovery использует immutable request receipt/snapshot, provider request reference и идемпотентную reconciliation. Известный provider request разрешено безопасно опрашивать и завершать один раз, включая restart worker между отправкой, сохранением provider ID и финализацией. Для каждой границы сбоя требуется подтверждённый durable state; невозможность восстановить ID не является разрешением отправить запрос заново. Слепой повтор initial submit, двойной ledger effect и автоматический refund при неизвестном факте принятия провайдером запрещены. Неразрешимая pre-request-ID ambiguity остаётся явным состоянием до отдельного продуктового правила или документированного provider contract.

## Отмена достоверно неотправленного платного анализа фото

**Решение владельца от 2026-09-24:** пользователь может отменить платный анализ
фото, если достоверно доказано, что запрос ещё не отправлялся провайдеру. При
такой отмене вся зарезервированная сумма токенов возвращается **ровно один раз**.
Отсутствие provider ID само по себе не доказывает отсутствие отправки. Состояния
с возможной или подтверждённой отправкой исключены; правило не разрешает refund
или resubmit для pre-ID `outcomeUnknown`/`ambiguous`. Политика goodwill для
неопределённого исхода остаётся `TBD`. Реализация `5a08efb` локально независимо
проверена: 10/10 actual PostgreSQL tests PASS. Это не закрывает отдельную
runtime-приёмку отмены и возврата.

## Текущая граница приёмки — 2026-09-25

Активен один `atlas-gerbi-expanded` на HTTP `5.42.126.71:3114`; прежний Daily Coach
остановлен с сохранением volumes/backup. Чат работает в fake-режиме, фото — через
GenAPI с отдельным consent, Push выключен. Node24.18.0/pnpm11.14.0 builds и
private storage gate подтверждены; текущие артефакты и отдельные browser-этапы — в
[constrained runtime](../07-deployment/gerbi-constrained-runtime.md).
Полный food acceptance продолжается; локальные исправления preview, подписи
загрузки и сохранения коррекции не заменяют повтор конкретного пользовательского
шага. Recovery после контролируемого сбоя и телефонная доставка ещё не приняты.
HTTP и desktop localhost SSH tunnel не закрывают HTTPS/phone gate. Прежний
марафонный цикл, дневные podium и запрет раскрытия накопленного итога сохраняются.

## Исторический срез реализации — 2026-09-24

Scope выше обязателен; готовность оценивается отдельно по реальному пути.

| Область | Подтверждённое свидетельство | Граница приёмки |
| --- | --- | --- |
| Frontend push | `baccdf5`: `/notifications`, opt-in, runtime VAPID config, device lookup/revoke, permission, расписание, service worker, защита при смене аккаунта | Доставка на физический телефон **NOT RUN** |
| Frontend история еды | `b28b2b3`: исправление/удаление подтверждённых записей, стабильные retries, reload и polling | Реальное фото → GenAPI → подтверждение → история в runtime **NOT ACCEPTED** |
| Локальные проверки UI | 94/94 web tests, typecheck и scoped lint PASS; production web build на `c9d9c6a` PASS, включая `/food` и `/notifications` | Node `26.0.0`, pnpm `11.19.0`; baseline Node 24.18.x / pnpm 11.14.x этим не проверен |
| Backend recovery | После четырёх P1 на `5723c53` интегрирован `31fd0c8`: local builds/lint PASS, worker40/API46/web94 PASS; PostgreSQL46 SKIPPED | Независимая перепроверка выявила оставшуюся гонку late-provider callback с sweep; закрытие и restart/runtime acceptance **NOT ACCEPTED** |
| Выпуск | Работа идёт в отдельных pilot-ветках | `main` и stable этим инкрементом не изменены; локальная сборка не разрешает считать пилот принятым |

Позднейшие backend commits сами по себе не закрывают приёмку: нужны результаты
повторной проверки конкретных findings и runtime на точном интегрированном commit.
Сценарии и форма доказательств — в
[ручной приёмке](../06-development/gerbi-marathon-manual-acceptance.md).

**Исторический blocker среза 2026-09-24 (не текущий статус):** сервер `5.42.126.71` не выдаёт SSH banner
на порту 22; pilot `3112` и stable `3100` отвечают timeout. Запрошена проверка
консоли владельцем. Это недоступный runtime, а не доказательство успешного
выпуска или установленной причины сбоя. Изменения `main`/stable не выполнялись.

Текущий runtime доступен на `3114`; прежние findings и timeout выше сохранены
как история. Позднейшие локальные recovery fixes проверены отдельно, но это
не заменяет runtime restart acceptance.
