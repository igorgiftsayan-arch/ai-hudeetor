# Герби-Марафон — ручная приёмка расширенного пилота

## Предусловия

- отдельная test topology собрана из точного commit ветки;
- migration image содержит `0012_gerbi_marathon_pilot.sql` и все последующие миграции точного проверяемого commit, migrations применены дважды;
- synthetic captain входит в `MARATHON_BOOTSTRAP_USER_IDS`, bootstrap включён только в test runtime;
- synthetic participant имеет тот же IANA timezone, что и марафон;
- API и worker используют одинаковые `AI_PROVIDER` и `IDENTITY_AI_PROVIDER_PROCESSING_VERSION`;
- для real-provider проверки используются только synthetic accounts и защищённый server env.

## Сценарий марафона

1. Зарегистрировать captain и participant, завершить onboarding и добавить дневной вес.
2. Captain создаёт marathon/team; сохранить join code вне логов и документации.
3. Participant присоединяется по коду; повтор с тем же idempotency key не создаёт вторую membership.
4. Оба пользователя читают `marathons/current`; пользователь без membership получает штатный `404 MARATHON_NOT_FOUND`.
5. Participant читает отчёт за вчера как `unknown`, сохраняет восемь отметок и после reload получает `reported` с тем же `markedCount`.
6. В первый день марафона отчёт за вчера возвращает `notApplicable`; отчёт за последний день допускается следующим утром.
7. Captain создаёт дневное задание. Participant отмечает выполнение и после reload видит `completed`.
   Новый key для задания прошлой даты получает `409 MARATHON_TASK_DATE_INVALID`,
   а точный replay успешной команды возвращает сохранённый response.
8. Добавить точные дневные веса вчера/сегодня: `dailyPercent` равен `(вчера − сегодня) / вчера × 100%`; при отсутствии любой даты остаётся `unknown`, более старая запись не используется.
9. Проверить team today: только участники своей команды, nullable display name, wellness `0..8`, self-reported task completion и до трёх podium groups. Равные результаты находятся в одной группе общего места; отсутствуют raw weight, chat, memory и cumulative totals.
10. Проверить, что baseline равен первой записи внутри периода марафона и не меняется после обычного исправления веса за этот день.
11. Повторить mutation requests с тем же key/payload и затем с тем же key/другим payload: business effect не дублируется, изменённый payload получает `409`.

## Real-provider сценарий

1. Убедиться, что до consent quick reply отклоняется без provider request и без списания.
2. Прочитать configured disclosure/version, сохранить consent текущей версии.
3. Отправить два последовательных сообщения через browser UI; обе операции должны завершиться `succeeded`, ledger — одной reservation/confirmation на операцию.
4. Подтвердить, что второй provider request получил history и построенный bounded memory context, не раскрывая его содержимое в логах или отчёте.
5. Проверить trace/request IDs, usage/latency и wallet balance; API key, prompt, response, raw provider error и private memory в обычных logs/outbox отсутствуют.

## После проверки

- отключить/удалить изолированный real-provider runtime env;
- stable `atlas-v01` оставить без изменений;
- зафиксировать точный branch/commit/image, результаты migrations, PostgreSQL/regression/browser проверок и все `TBD`.

## Фото еды и подтверждённая история — обязательная сквозная проверка

1. На synthetic аккаунте с актуальным consent открыть `/food`, увидеть цену,
   загрузить допустимую фотографию и получить **реальный GenAPI** результат.
   Fake/fixture результат отмечается отдельно и не закрывает этот шаг.
2. Проверить блюдо/состав, неопределённость и suitability только по известному
   профилю/правилам с источником. Для отсутствующих данных — `insufficientData`;
   дополнительных правил Герби без решения владельца нет.
3. До ответа «Съели ли вы это?» записи употребления нет. Исправить распознавание,
   подтвердить состав и локальные дату/время; повтор запроса не создаёт дубль.
4. После reload увидеть запись истории. Исправить её состав/время, сохранить и
   проверить reload; затем удалить и проверить отсутствие в истории и контексте
   как факта употребления. Второй аккаунт не читает/меняет чужую запись.
5. Проверить ошибку загрузки, consent, отклонение файла, задержку анализа и
   неоднозначный сетевой ответ confirmation/edit/delete. Безопасный повтор
   сохраняет исходный payload/key; UI не выдаёт неизвестный исход за успех.
6. Проверить ledger один раз на AI-операцию и приватность оригинала/URL. Наличие
   фото не становится фактом употребления; наблюдения по весу не выдаются за
   причину изменений. Аналитический экран не считается готовым этой проверкой.

## Телефонный Web Push — обязательная физическая проверка

1. Зафиксировать модель/OS/browser телефона, точный origin и commit. Нужен
   действующий доверенный HTTPS; HTTP по IP не закрывает acceptance. Для iPhone
   установить PWA на экран «Домой» и открыть оттуда.
2. Открыть `/notifications`: до явного действия запрос permission не возникает.
   Проверить `unsupported`, `denied`, отсутствие VAPID config и HTTP guidance.
   Даже если у аккаунта есть другие subscriptions, denied этого телефона
   остаётся denied, а не «уведомления включены».
3. Явно разрешить уведомления, подключить телефон и сохранить `enabled`,
   `localTime`, `timezone`. После reload UI показывает фактический статус endpoint
   этого устройства; read-only lookup не создаёт подписку.
4. В выбранное время получить уведомление на физическом телефоне, включая
   закрытое приложение. На экране нет веса, еды или личного текста. Нажатие
   открывает разрешённый маршрут приложения; повтор deliveryId не дублирует
   уведомление. Зафиксировать фактический результат, не предположение.
5. Отключить только это устройство; другие подписки не отзываются. Отдельно
   выключить расписание аккаунта. Проверить смену timezone, expiry/revoke,
   logout и смену аккаунта: доставка прежнего аккаунта на телефоне прекращается.
6. Зафиксировать реальную доставку отдельно для каждой проверенной платформы;
   desktop mock/service-worker unit test не заменяет iOS/Android acceptance.

## Автоматическое восстановление — обязательная runtime проверка

1. На разрешённом тестовом контуре остановить worker в контролируемых точках:
   до submit, после возможного принятия provider, после сохранения известного ID
   и до финализации. Зафиксировать receipt/state и точный commit без private data.
2. После restart известный provider ID опрашивается и завершается ровно один раз;
   исходный snapshot сохраняется, результат и ledger не дублируются.
3. Проверить поздний callback одновременно с recovery sweep: нельзя принять
   техническую ошибку/refund и затем сохранить late success с неверной экономикой.
4. При неоднозначном initial submit без ID нет слепого resubmit и автоматического
   refund; виден `outcomeUnknown`/соответствующий неопределённый исход. Отдельная
   goodwill policy остаётся `TBD`, тест не придумывает её.
5. Подтверждённая техническая ошибка возвращает резерв один раз; business failure
   корректного результата не подменяется техническим refund. Реальный provider
   contract и durable state должны подтверждать каждое восстановление.

## Текущий результат, не отметка о выполнении сценариев

Срез 2026-09-24: frontend `baccdf5`/`b28b2b3`, 94/94 web tests, typecheck и scoped
lint PASS. Production web build `c9d9c6a` PASS локально на Node26.0.0/pnpm11.19.0;
это не проверка baseline Node24.18.x/pnpm11.14.x. Backend `31fd0c8`: local
builds/lint PASS, worker40/API46/web94 PASS, PostgreSQL46 SKIPPED. Независимый
review оставил гонку late-provider callback с sweep, поэтому recovery acceptance
не закрыта. Реальная цепочка food, restart/runtime и физическая phone delivery
**NOT ACCEPTED**; телефонная доставка **NOT RUN**.

Сервер `5.42.126.71`: порт22 без SSH banner, pilot3112/stable3100 timeout;
запрошена консоль владельца. Причина не установлена, runtime-проверка блокирована.
Домен → этот IP, тип телефона, дополнительные Герби-правила и pre-ID goodwill
policy — `TBD`. Main/stable этим инкрементом не изменены.

Для каждого выполненного шага записывать точный commit/image, среду, время,
аккаунт-роль без PII, ожидаемый и наблюдённый результат, PASS/FAIL/NOT RUN и
ссылку на безопасное свидетельство. Пустой шаг не означает PASS.
